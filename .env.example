# Cesantoni Experience - Variables de Entorno
# Copia este archivo a .env y configura los valores

# Puerto del servidor (Railway lo asigna automáticamente)
PORT=3000

# URL base para QR codes (cambiar en producción)
# Railway: https://tu-proyecto.up.railway.app
BASE_URL=http://localhost:3000

# Google API Key para Veo 3.1 (video generation)
# Obtener en: https://console.cloud.google.com/apis/credentials
GOOGLE_API_KEY=tu_api_key_aqui

# Path a FFmpeg (opcional, por defecto usa el del sistema)
# FFMPEG_PATH=ffmpeg

# Google Cloud Storage para videos (opcional pero recomendado en producción)
# Sin GCS los videos se guardan localmente (se pierden en Render/Railway)
GCS_BUCKET=cesantoni-videos

# Opción 1: Path al archivo JSON de service account
# GCS_KEY_FILE=/path/to/service-account.json

# Opción 2: Credenciales como JSON string (para Render/Railway)
# GCS_CREDENTIALS={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}
